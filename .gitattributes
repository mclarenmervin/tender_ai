# Keep repository text files normalized to LF across Windows and Linux.
* text=auto eol=lf

# Windows helper scripts should keep CRLF when checked out.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
